# جریان داده سازمانی - پیاده‌سازی کامل

## خلاصه
این سند توضیح می‌دهد که چگونه پیچ‌های ارسالی به صورت خودکار به سازمان‌ها لینک می‌شوند و ادمین‌های سازمانی چگونه به تمام داده‌های شرکت‌کنندگان دسترسی دارند.

## تغییرات اعمال شده

### 1. بارگذاری خودکار اطلاعات سازمان
**فایل**: `contexts/AuthContext.tsx`

وقتی کاربر لاگین می‌کند:
- سیستم عضویت‌های سازمانی فعال را چک می‌کند
- اگر کاربر عضو سازمانی باشد، اطلاعات سازمان بارگذاری می‌شود
- این اطلاعات در تمام اپلیکیشن در دسترس است

### 2. لینک خودکار پیچ‌ها به سازمان
**فایل**: `hooks/usePitchAnalysis.ts`

وقتی کاربر پیچ ارسال می‌کند:
- سیستم چک می‌کند آیا کاربر عضو سازمانی است
- اگر بله، به صورت خودکار `organizationId` و `programId` اضافه می‌شود
- این اطلاعات با پیچ ذخیره می‌شود

### 3. ذخیره‌سازی پیچ‌ها
**فایل**: `app/api/pitch-submissions/route.ts`

دو کالکشن برای ذخیره پیچ‌ها:

#### الف. `pitchSubmissions` (نمای سازمانی)
- تمام پیچ‌ها با لینک سازمانی
- ادمین‌ها می‌توانند ببینند
- شامل:
  - متن کامل پیچ
  - تحلیل کامل PerfectPitch
  - نمرات
  - تاریخ و زمان

#### ب. `users/{uid}/sessions` (تاریخچه شخصی)
- تاریخچه شخصی هر کاربر
- همان داده‌های pitchSubmissions
- برای نمای تاریخچه کاربر

### 4. دسترسی ادمین سازمان
**فایل**: `app/api/organizations/[orgId]/pitches/route.ts`

ادمین‌های سازمانی می‌توانند:
```
GET /api/organizations/{orgId}/pitches
```

این API تمام پیچ‌هایی که `organizationId` دارند را برمی‌گرداند:
- ✅ تمام پیچ‌های شرکت‌کنندگان
- ✅ متن کامل پیچ
- ✅ نتایج تحلیل کامل
- ✅ نمرات و معیارها
- ✅ تاریخ و زمان

### 5. نمایش در رابط کاربری
**فایل**: `components/dashboard/DashboardContent.tsx`

وقتی کاربر عضو سازمان است:
- در هدر یک بج با نام سازمان نمایش داده می‌شود
- نقطه سبز نشان‌دهنده عضویت فعال است
- مثال: `🟢 نام سازمان`

## جریان کامل داده

```
کاربر لاگین می‌کند
    ↓
عضویت سازمانی بارگذاری می‌شود
    ↓
کاربر پیچ ارسال می‌کند
    ↓
[organizationId + programId به صورت خودکار اضافه می‌شود]
    ↓
ذخیره در pitchSubmissions (با داده‌های سازمانی)
    ↓
ذخیره در users/{uid}/sessions (تاریخچه شخصی)
    ↓
ادمین سازمان کوئری می‌زند
    ↓
GET /api/organizations/{orgId}/pitches
    ↓
تمام پیچ‌های شرکت‌کنندگان برگردانده می‌شود
```

## حریم خصوصی و کنترل دسترسی

### نمای شرکت‌کننده
- ✅ می‌تواند پیچ‌های خودش را در تاریخچه ببیند
- ✅ می‌تواند بج سازمان را در هدر ببیند
- ❌ نمی‌تواند پیچ‌های شرکت‌کنندگان دیگر را ببیند

### نمای ادمین سازمان
- ✅ می‌تواند تمام پیچ‌های شرکت‌کنندگان را ببیند
- ✅ می‌تواند بر اساس برنامه، نمره، تاریخ فیلتر کند
- ✅ دسترسی کامل به متن و تحلیل دارد
- ✅ می‌تواند آنالیتیکس و معیارها را ببیند

### جداسازی داده
- پیچ‌های بدون `organizationId` کاملاً خصوصی هستند
- فقط پیچ‌های با `organizationId` برای ادمین‌ها قابل مشاهده است
- کاربرانی که عضو سازمان نیستند، پیچ‌های کاملاً خصوصی دارند

## APIهای موجود

### برای شرکت‌کنندگان
```typescript
// ارسال پیچ (به صورت خودکار به سازمان لینک می‌شود)
POST /api/analyze-pitch

// دریافت پیچ‌های خودم
GET /api/pitch-submissions
```

### برای ادمین‌های سازمانی
```typescript
// دریافت تمام پیچ‌های سازمان
GET /api/organizations/{orgId}/pitches

// دریافت آنالیتیکس سازمان
GET /api/organizations/{orgId}/analytics

// دریافت لیست شرکت‌کنندگان با معیارها
GET /api/organizations/{orgId}/participants
```

## چک‌لیست پیاده‌سازی

✅ AuthContext عضویت سازمانی را بارگذاری می‌کند
✅ کانتکست سازمانی در تمام اپ در دسترس است
✅ پیچ‌ها به صورت خودکار به سازمان لینک می‌شوند
✅ کالکشن pitchSubmissions داده‌های سازمانی را ذخیره می‌کند
✅ بج سازمان در هدر نمایش داده می‌شود
✅ ادمین می‌تواند تمام پیچ‌های شرکت‌کنندگان را ببیند
✅ سرویس آنالیتیکس بر اساس سازمان فیلتر می‌کند
✅ حریم خصوصی برای کاربران غیرسازمانی حفظ می‌شود

## تست

### سناریو 1: عضو سازمان
1. کاربر دعوت‌نامه سازمان را قبول می‌کند
2. کاربر لاگین می‌کند
3. هدر بج سازمان را نشان می‌دهد
4. کاربر پیچ ارسال می‌کند
5. ادمین می‌تواند پیچ را در پنل سازمانی ببیند ✅

### سناریو 2: غیرعضو
1. کاربر لاگین می‌کند (بدون سازمان)
2. بج سازمانی در هدر نیست
3. کاربر پیچ ارسال می‌کند
4. پیچ خصوصی می‌ماند (بدون organizationId)
5. ادمین نمی‌تواند پیچ را ببیند ✅

### سناریو 3: نمای ادمین
1. ادمین به پنل سازمانی لاگین می‌کند
2. تب "Pitches" را می‌بیند
3. تمام پیچ‌های شرکت‌کنندگان را می‌بیند
4. می‌تواند بر اساس برنامه، نمره، تاریخ فیلتر کند
5. می‌تواند تحلیل کامل هر پیچ را ببیند ✅

## فایل‌های تغییر یافته

1. ✅ `contexts/AuthContext.tsx` - بارگذاری کانتکست سازمانی
2. ✅ `hooks/usePitchAnalysis.ts` - لینک خودکار به سازمان
3. ✅ `components/dashboard/DashboardContent.tsx` - نمایش بج سازمان
4. ✅ `app/api/analyze-pitch/route.ts` - دریافت organizationId
5. ✅ `app/api/pitch-submissions/route.ts` - ذخیره و کوئری پیچ‌ها (جدید)
6. ✅ `lib/perfectPitchTypes.ts` - اضافه کردن فیلدهای سازمانی

## نتیجه

اکنون:
- ✅ وقتی کسی به سازمان دعوت می‌شود و لاگین می‌کند، بج سازمان در هدر نمایش داده می‌شود
- ✅ تمام پیچ‌های او به صورت خودکار به سازمان لینک می‌شوند
- ✅ ادمین سازمان می‌تواند تمام پیچ‌ها، تحلیل‌ها، و فایل‌ها را ببیند
- ✅ حریم خصوصی برای کاربران غیرسازمانی حفظ می‌شود
