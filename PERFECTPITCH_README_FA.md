# سیستم PerfectPitch - راهنمای فارسی

## 🎯 خلاصه

یک سیستم کامل تحلیل پیچ دک با هوش مصنوعی که در سه مرحله، ارزیابی حرفه‌ای سرمایه‌گذار را شبیه‌سازی می‌کند.

## 📁 فایل‌های ایجاد شده

### فایل‌های اصلی
1. `lib/perfectPitchTypes.ts` - تعریف تایپ‌های TypeScript
2. `app/api/perfect-pitch/route.ts` - API endpoint اصلی
3. `components/PerfectPitchResult.tsx` - کامپوننت نمایش نتایج
4. `hooks/usePerfectPitch.ts` - React hook
5. `app/perfect-pitch-demo/page.tsx` - صفحه دمو

### مستندات
6. `docs/PERFECTPITCH_PROMPTS.md` - پرامپت‌های مهندسی شده
7. `docs/PERFECTPITCH_IMPLEMENTATION.md` - راهنمای پیاده‌سازی
8. `docs/PERFECTPITCH_SAMPLE_PITCH.md` - نمونه‌های تست
9. `docs/PERFECTPITCH_CHANGELOG.md` - تغییرات

## 🚀 نحوه استفاده

### مرحله 1: تست دمو
```
http://localhost:3000/perfect-pitch-demo
```

### مرحله 2: استفاده از نمونه محتوا
از فایل `docs/PERFECTPITCH_SAMPLE_PITCH.md` محتوای نمونه را کپی کنید.

### مرحله 3: تحلیل
1. محتوای پیچ دک را paste کنید
2. مرحله (pre-seed/seed/series-a) را انتخاب کنید
3. صنعت را وارد کنید
4. روی "Analyze Pitch Deck" کلیک کنید

### مرحله 4: مشاهده نتایج
چهار تب:
- **Overview**: امتیاز، رای نهایی، سیگنال‌ها
- **Deep Dive**: کارت امتیازی، بازسازی استارتاپ
- **Action Plan**: چک‌لیست اولویت‌بندی شده
- **Investor Tests**: شش تست حیاتی

## 🎨 معماری سه مرحله‌ای

### مرحله 1: شبیه‌سازی سرمایه‌گذار
**مدل**: GPT-4o (قدرتمندترین برای استدلال عمیق)

**خروجی**:
- بازسازی واقعیت استارتاپ (مشکل، راه‌حل، بازار)
- امتیاز کیفیت ایده (0-10)
- امتیاز کیفیت پیچ (0-10)
- سیگنال‌های سرمایه‌گذار (مثبت/منفی/حیاتی)
- تطبیق الگو با استارتاپ‌های موفق/ناموفق
- آمادگی سرمایه‌گذاری

**هزینه**: $0.10-0.30 به ازای هر تحلیل

### مرحله 2: موتور تصمیم‌گیری
**مدل**: GPT-4o-mini (سبک‌تر برای ساختاردهی)

**خروجی**:
- کارت امتیازی 8 بعدی (مقیاس 1-10)
- تشخیص شکاف (بزرگترین شکاف، سریع‌ترین برد، توهمات خطرناک)
- چک‌لیست اولویت‌بندی شده (بالا/متوسط/پایین)
- منطق تصمیم‌گیری
- پتانسیل بهبود

**هزینه**: $0.02-0.05 به ازای هر تحلیل

### مرحله 3: اعتبارسنجی نهایی
**مدل**: GPT-4o (قدرتمند برای کیفیت اعتبارسنجی)

**خروجی**:
- شش تست حیاتی (هر کدام 0-10):
  1. تست سازگاری
  2. تست استرس فرضیات
  3. تست پوشش اعتراضات
  4. تست وضوح تحت فشار
  5. تست باورپذیری بازار
  6. تست انسجام داستان
- امتیاز آمادگی نهایی (0-100)
- باند آمادگی (رد/ضعیف/بررسی/آماده برای بررسی انسانی)
- رای دروازه سرمایه‌گذار (قبول/رد)

**هزینه**: $0.05-0.15 به ازای هر تحلیل

## 💻 نحوه استفاده در کد

### روش 1: استفاده از Hook
```typescript
import { usePerfectPitch } from '@/hooks/usePerfectPitch'

function MyComponent() {
  const { analyze, analysis, loading, error } = usePerfectPitch()

  const handleAnalyze = () => {
    analyze({
      pitchDeckContent: 'محتوای پیچ دک شما...',
      stage: 'seed',
      industry: 'Tech/SaaS'
    })
  }

  if (analysis) {
    return <PerfectPitchResult analysis={analysis} onReset={reset} />
  }

  return (
    <button onClick={handleAnalyze} disabled={loading}>
      {loading ? 'در حال تحلیل...' : 'تحلیل پیچ دک'}
    </button>
  )
}
```

### روش 2: فراخوانی مستقیم API
```typescript
const response = await fetch('/api/perfect-pitch', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    pitchDeckContent: 'محتوای پیچ دک...',
    stage: 'seed',
    industry: 'Tech/SaaS',
    targetInvestorType: 'VC'
  })
})

const analysis = await response.json()
```

### روش 3: استفاده از کامپوننت
```typescript
import PerfectPitchResult from '@/components/PerfectPitchResult'

<PerfectPitchResult 
  analysis={analysisData} 
  onReset={() => console.log('Reset')} 
/>
```

## 📊 عملکرد و هزینه

| مرحله | مدل | هزینه | زمان |
|-------|-----|-------|------|
| مرحله 1 | GPT-4o | $0.10-0.30 | 8-15 ثانیه |
| مرحله 2 | GPT-4o-mini | $0.02-0.05 | 3-6 ثانیه |
| مرحله 3 | GPT-4o | $0.05-0.15 | 5-10 ثانیه |
| **جمع** | - | **$0.17-0.50** | **16-31 ثانیه** |

## ✨ ویژگی‌های کلیدی

### خروجی مرحله 1
- ✅ بازسازی استارتاپ (مشکل، راه‌حل، مشتری، بازار، مدل کسب‌وکار)
- ✅ امتیاز کیفیت ایده (0-10)
- ✅ امتیاز کیفیت پیچ (0-10)
- ✅ سیگنال‌های سرمایه‌گذار
- ✅ تطبیق الگو
- ✅ آمادگی سرمایه‌گذاری

### خروجی مرحله 2
- ✅ کارت امتیازی 8 بعدی
- ✅ تشخیص شکاف
- ✅ چک‌لیست اولویت‌بندی شده
- ✅ منطق تصمیم‌گیری
- ✅ پتانسیل بهبود

### خروجی مرحله 3
- ✅ شش تست حیاتی سرمایه‌گذار
- ✅ امتیاز آمادگی نهایی (0-100)
- ✅ باند آمادگی
- ✅ رای دروازه (قبول/رد با سطح اطمینان)

## 🎨 ویژگی‌های رابط کاربری

- ✅ تم تیره مدرن با glassmorphism
- ✅ نمایش امتیازات با انیمیشن
- ✅ باندهای آمادگی با رنگ‌بندی
- ✅ ناوبری چهار تبه
- ✅ نشانگرهای پیشرفت
- ✅ طراحی ریسپانسیو
- ✅ کپی به کلیپ‌بورد
- ✅ قابلیت ریست

## 📖 مستندات

### برای توسعه‌دهندگان
- **راهنمای پیاده‌سازی**: `docs/PERFECTPITCH_IMPLEMENTATION.md`
- **تعریف تایپ‌ها**: `lib/perfectPitchTypes.ts`
- **API Route**: `app/api/perfect-pitch/route.ts`

### برای مهندسان پرامپت
- **مهندسی پرامپت**: `docs/PERFECTPITCH_PROMPTS.md`
- سه پرامپت کامل با دستورالعمل‌های سیستم
- مشخصات ورودی/خروجی
- نکات بهینه‌سازی هزینه

### برای تست
- **نمونه پیچ‌ها**: `docs/PERFECTPITCH_SAMPLE_PITCH.md`
- سه مورد تست (قوی/ضعیف/متوسط)
- محدوده امتیازات مورد انتظار
- چک‌لیست تست

## 🔧 یکپارچه‌سازی با سیستم موجود

### گزینه 1: جایگزینی تحلیل فعلی
فراخوانی‌های `analyzePitchDeck` فعلی را با hook `usePerfectPitch` جایگزین کنید.

### گزینه 2: افزودن به عنوان حالت جایگزین
یک toggle برای سوئیچ بین تحلیل استاندارد و PerfectPitch اضافه کنید.

### گزینه 3: رویکرد ترکیبی
از PerfectPitch برای تحلیل دقیق و از سیستم موجود برای بازخورد سریع استفاده کنید.

## 🚦 مراحل بعدی

### فوری (امروز)
1. ✅ تست صفحه دمو با محتوای نمونه
2. ✅ بررسی تکمیل هر سه مرحله
3. ✅ تأیید نمایش صحیح UI

### کوتاه‌مدت (این هفته)
1. یکپارچه‌سازی با pitch recorder موجود
2. افزودن به داشبورد اصلی
3. جمع‌آوری بازخورد کاربران

### میان‌مدت (این ماه)
1. افزودن قابلیت export (PDF/DOCX)
2. پیاده‌سازی تاریخچه تحلیل
3. افزودن حالت مقایسه

### بلندمدت (سه ماه آینده)
1. پشتیبانی چند زبانه
2. معیارهای ارزیابی سفارشی
3. تطبیق سرمایه‌گذار
4. امتیاز پیش‌بینی موفقیت تأمین مالی

## 🎯 چه چیزی آن را ویژه می‌کند

### 1. معماری سه مرحله‌ای
جداسازی استدلال، تصمیم‌گیری و اعتبارسنجی برای نتایج بهینه.

### 2. ارزیابی درجه سرمایه‌گذار
شبیه‌سازی ارزیابی VC با تجربه، نه بازخورد عمومی.

### 3. خروجی قابل اجرا
چک‌لیست اولویت‌بندی شده مستقیماً به اعتراضات سرمایه‌گذار نگاشت می‌شود.

### 4. منطق دروازه‌بانی
رای باینری (قبول/رد) با سطح اطمینان.

### 5. شش تست حیاتی
- سازگاری
- فرضیات
- اعتراضات
- وضوح
- باورپذیری
- انسجام

### 6. JSON ساختاریافته
تمام خروجی‌ها برای پردازش downstream قابل خواندن توسط ماشین هستند.

## 🐛 عیب‌یابی

### تحلیل خیلی طول می‌کشد
- طول محتوای پیچ دک را بررسی کنید (توصیه: < 10,000 کاراکتر)
- اعتبار OpenAI API key را تأیید کنید
- اتصال شبکه را بررسی کنید

### امتیازات غیرمنتظره
- اطمینان حاصل کنید محتوای پیچ دک کامل و خوب فرمت شده است
- مرحله و صنعت را به درستی تنظیم کنید
- خروجی مرحله 1 را برای دقت بازسازی بررسی کنید

### خطاهای پارس JSON
- این نشان می‌دهد OpenAI JSON نامعتبر برگردانده
- وضعیت OpenAI API را بررسی کنید
- تحلیل را دوباره امتحان کنید
- تنظیم پارامترهای temperature را در نظر بگیرید

## 📞 پشتیبانی

### مستندات
- همه پرامپت‌ها: `docs/PERFECTPITCH_PROMPTS.md`
- پیاده‌سازی: `docs/PERFECTPITCH_IMPLEMENTATION.md`
- نمونه‌ها: `docs/PERFECTPITCH_SAMPLE_PITCH.md`
- تغییرات: `docs/PERFECTPITCH_CHANGELOG.md`

### تست
- صفحه دمو: `/perfect-pitch-demo`
- محتوای نمونه ارائه شده
- محدوده امتیازات مورد انتظار مستند شده

## ✅ خلاصه

شما اکنون یک سیستم کامل، آماده تولید، تحلیل پیچ درجه سرمایه‌گذار دارید که:

- ✅ از پایپلاین AI سه مرحله‌ای استفاده می‌کند
- ✅ بینش‌های ساختاریافته و قابل اجرا ارائه می‌دهد
- ✅ رای دروازه باینری ارائه می‌دهد
- ✅ شامل کامپوننت‌های UI مدرن است
- ✅ مستندات جامع دارد
- ✅ شامل نمونه‌های تست است
- ✅ آماده یکپارچه‌سازی است

**پیاده‌سازی کل**: 9 فایل، کاملاً مستند، آماده تولید.

---

**وضعیت**: ✅ کامل  
**تاریخ**: 22 ژانویه 2026  
**نسخه**: 1.0.0  
**آماده برای**: تست → یکپارچه‌سازی → تولید
